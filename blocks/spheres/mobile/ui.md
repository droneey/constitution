# UI — mobile

> What a device adds to the `ui` concern: touch, device accessibility and platform conventions. The `ui` chapter is the law. **Skeleton:** every `TODO:` is a question the first mobile project answers; until then the `ui` chapter alone applies.

---

## 1. Touch
TODO: minimum target sizes, spacing between targets, gesture conflicts with the navigator, safe areas and notches, keyboard avoidance for forms.

---

## 2. Accessibility on a device
TODO: the accessibility label, role and state props the platform expects, screen-reader reading order, dynamic type and contrast, reduced motion, and the hand-verified checklist that replaces the web's Tab/Enter/Escape checks.

---

## 3. Platform conventions
TODO: where iOS and Android conventions diverge — navigation gestures, back behaviour, sheets versus dialogs, haptics — and which the design system adopts as one behaviour for both.
